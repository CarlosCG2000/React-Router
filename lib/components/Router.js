import{jsx as _jsx}from"react/jsx-runtime";import{EVENTS}from"../utils/consts";import{Children,useEffect,useState,useMemo}from"react";import{match}from"path-to-regexp";import{getCurrentPath}from"../utils/getCurrentPath";import PropTypes from"prop-types";export function Router({children,routes=[],defaultComponent:DefaultComponent=()=>null}){const[currentPath,setCurrentPath]=useState(getCurrentPath());useEffect(()=>{const onLocationChange=()=>{setCurrentPath(getCurrentPath())};window.addEventListener(EVENTS.PUSHSTATE,onLocationChange);window.addEventListener(EVENTS.POPSTATE,onLocationChange);return()=>{window.removeEventListener(EVENTS.PUSHSTATE,onLocationChange);window.removeEventListener(EVENTS.POPSTATE,onLocationChange)}},[]);const routesFromChildren=useMemo(()=>{return Children.map(children,({props,type})=>{const{name}=type;const isRoute=name==="Route";if(!isRoute)return null;return props})},[children]);const routesToUse=useMemo(()=>{return routes.concat(routesFromChildren).filter(Boolean)},[routes,routesFromChildren]);const{Page,routeParams}=useMemo(()=>{const result={Page:DefaultComponent,routeParams:{}};routesToUse.find(({path})=>{if(path===currentPath){result.Page=routesToUse.find(r=>r.path===path)?.Component||DefaultComponent;return true}const matcherUrl=match(path,{decode:decodeURIComponent});const matched=matcherUrl(currentPath);if(!matched)return false;result.Page=routesToUse.find(r=>r.path===path)?.Component||DefaultComponent;result.routeParams=matched.params;return true});return result},[routesToUse,currentPath,DefaultComponent]);return _jsx(Page,{routeParams:routeParams})}Router.propTypes={children:PropTypes.node,routes:PropTypes.arrayOf(PropTypes.shape({path:PropTypes.string.isRequired,Component:PropTypes.elementType.isRequired})),defaultComponent:PropTypes.elementType};